<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rifqi.footballapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".features.Teams.Search.SearchTeamActivity"></activity>
        <activity
            android:name=".features.DetailTeam.DetailTeamActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".features.Standings.StandingsActivity" />
        <activity android:name=".features.Teams.TeamsActivity" />
        <activity
            android:name=".features.MatchSchedule.DetailMatch.DetailMatchActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".features.MatchSchedule.SearchMatch.SearchMatchActivity" />
        <activity android:name=".features.MatchSchedule.MatchActivity" />
        <activity
            android:name=".features.DetailLeague.DetailLeagueActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>